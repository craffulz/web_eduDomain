/* eslint-disable no-unused-vars */
/**
 * Componente que muestra los diferentes clientes
 */
import React from 'react'
import { useSpring } from '@react-spring/web'
const Clients = () => {
    const [styles, api] = useSpring(() => ({

    }))
  return (
    <div>Clients</div>
  )
}

export default Clients